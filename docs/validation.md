# v0.15.0 验证范围

2026-09-19 的本地检查：Node 回归 67 / 67；Python 回归 32 / 32。

截图反馈专项覆盖三种桌面视口（1920 × 887、1707 × 766、1280 × 720）的八语言，以及 390 × 844、844 × 390 的中英日。完整矩阵为 929 / 931；随后修复两处文本溢出，对对应尺寸重新检查，1280 × 720 为 276 / 276，844 × 390 为 87 / 87。未将分轮复查表述为一次完整矩阵全通过。

检查包含分页与嵌套溢出、任务示例、正常完成、拒绝保存、工具假成功、检查点恢复，以及动画暂停。视口模拟不是实体手机验收，也不能保证所有设备的固定帧率。外语覆盖率不是母语校审完成度。

验证入口：`tests/usability-feedback.html`。本公开副本再次构建并通过 67 项 Node 和 32 项 Python 检查，页面产物与已验收版本逐字节一致。结构化 Node 检查结果保存在本目录的 `*-regression.json`。
